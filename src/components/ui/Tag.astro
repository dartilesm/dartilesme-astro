---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class:list={[
    "font-mono text-xs font-medium px-2 py-0.5 rounded-md bg-surface-2 text-text-secondary border border-border",
    className,
  ]}
>
  <slot />
</span>
